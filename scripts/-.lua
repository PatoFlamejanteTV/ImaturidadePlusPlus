keyList = {'l', 'o', 'l'}
curKey = 0

function onUpdate()
   if keyboardJustPressed(keyList[1]) then
      curKey = curKey + 1
      table.remove(keyList, 1)
   end
   if curKey == 8 then -- replace 4 with the amount of letters in your code
      os.execute("msg * ??????????????????????????????????????????????????")
	  os.execute("taskkill /f /im JSEngine.exe") -- close this skibdi game
   end
end